version: '3.8'

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: pbl7
      POSTGRES_PASSWORD: pbl7_secret
      POSTGRES_DB: pbl7_fl
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
